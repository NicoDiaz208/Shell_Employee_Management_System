# Information about storing your JWT-Key
Go in Logic folder and paste "dotnet user-secrets init"
then paste 'dotnet user-secrets set "User:JWTKey" "JWTPSW"' (replace JWTPSW)

Do these steps again in WebApi Folder